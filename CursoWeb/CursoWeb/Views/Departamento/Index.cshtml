@model IEnumerable<CursoWeb.Models.departamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DepartamentoNome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DepartamentoSigla)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DepartamentoNome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DepartamentoSigla)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepartamentoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepartamentoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepartamentoID })
        </td>
    </tr>
}

</table>
